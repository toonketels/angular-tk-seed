/**
 * Reusable code for all layers. Might be turned into proper module and put into vendor dir.
 */
define(['angular'], function(angular) {
    'use strict';

    return angular.module('tk.lib', []);
});
